// The API client follows the axios-swagger-helper output contract.
// Regenerate typed endpoint wrappers from the running backend with `npm run api`.
export const openApiUrl = `${import.meta.env.VITE_BACKEND_URL || "/api"}/openapi.json`;
